# Generated by the pipeline. Everything here is reproducible by `python run_all.py`.
raw/
run.log
__pycache__/

# build/, tables/ and figures/ ARE tracked, deliberately -- a clone should show you
# the results before you run anything, and `git diff` after a run then tells you
# exactly what your edit changed. They total ~1.1 MB.
